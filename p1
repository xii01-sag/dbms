CREATE USER 'vijay'@'localhost' IDENTIFIED BY '123456789';
GRANT ALL PRIVILEGES ON *.* To 'vijay'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;

CREATE DATABASE expl;
USE expl;

CREATE TABLE employee01 (
    EMPNO int,
    ENAME varchar(255),
    JOB varchar(255),
    MANAGER_NO int,
    SAL decimal(10, 2),
    COMISSION DECIMAL(10, 2)
);

START TRANSACTION;
INSERT INTO employee01 (EMPNO, ENAME, JOB, MANAGER_NO, SAL, COMISSION)
VALUES (1, 'KAVANA SHETTY', 'MANAGER', NULL, 5000.00, 1000.00);
COMMIT;

SELECT * FROM employee01;

START TRANSACTION;
INSERT INTO employee01 (EMPNO, ENAME, JOB, MANAGER_NO, SAL, COMISSION)
VALUES (2, 'Ram Charan', 'Developer', 1, 4000.00, NULL);

INSERT INTO employee01 (EMPNO, ENAME, JOB, MANAGER_NO, SAL, COMISSION)
VALUES (3, 'Honey Singh', 'Sales Person', 2, 3000.00, 500.00);

SET SQL_SAFE_UPDATES = 0;
SELECT * from employee01;

DELETE FROM employee01 WHERE ENAME = 'KAVANA SHETTY';
SELECT * from employee01;

ROLLBACK;
SELECT * from employee01;

ALTER TABLE employee01 ADD CONSTRAINT PK_employee PRIMARY KEY (EMPNO);

ALTER TABLE employee01 
MODIFY ENAME varchar(255) NOT NULL,
MODIFY JOB varchar(255) NOT NULL;

INSERT INTO employee01 (EMPNO, ENAME, JOB, MANAGER_NO, SAL, COMISSION)
VALUES (5, 'RAJAN', 'MANAGER', NULL, 4000.00, 1000.00);

SELECT * FROM employee01;
